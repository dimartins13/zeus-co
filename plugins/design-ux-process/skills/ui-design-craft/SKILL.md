---
name: ui-design-craft
description: "Craft visual de UI — gateway pra 14 sub-skills do designer-skills (Owl Listener). Use SEMPRE que o trabalho envolver craft visual de ui. Frases-gatilho: 'hierarquia visual', 'grid de layout', 'sistema de espaçamento', 'escala tipográfica', 'sistema de cores', 'dark mode', 'design responsivo', 'visualização de dados', 'leis de UX', 'Gestalt'. Leia a sub-skill certa em resources/ antes de executar."
---

# Craft visual de UI (ui-design-craft)

Este gateway agrupa 14 sub-skills de **craft visual de ui**. Cada sub-skill é um método completo e autocontido.

## Como usar

1. Identifique na tabela abaixo a(s) sub-skill(s) que casam com a tarefa.
2. Leia o arquivo `resources/<nome>/SKILL.md` (relativo a esta pasta) da(s) escolhida(s) — leia ANTES de produzir qualquer entregável.
3. Siga o método da sub-skill. Se duas se aplicam, leia ambas e combine.
4. Responda no idioma do usuário (PT-BR por padrão), mesmo que a sub-skill esteja em inglês.

## Sub-skills disponíveis

| Sub-skill | O que faz |
|---|---|
| `aesthetic-usability` | Apply the Aesthetic-Usability Effect — visually consistent, polished interfaces are perceived as more usable. |
| `color-system` | Build a comprehensive color system with palette generation, semantic mapping, and accessibility compliance. |
| `dark-mode-design` | Design effective dark mode interfaces with proper color adaptation, contrast, and elevation. |
| `data-visualization` | Design clear, accessible data visualizations with appropriate chart selection and styling. |
| `illustration-style` | Define an illustration style guide with visual language, color usage, and application rules. |
| `law-of-common-region` | Apply the Law of Common Region to group elements using containers, backgrounds, and boundaries. |
| `law-of-proximity` | Apply the Law of Proximity to group related elements through spatial relationships. |
| `layout-grid` | Define responsive layout grid systems with columns, gutters, margins, and breakpoint behavior. |
| `readable-measure` | Set optimal line lengths for readability across typography scales and responsive layouts. |
| `responsive-design` | Design adaptive layouts and interactions that work across all screen sizes and input methods. |
| `spacing-system` | Create a consistent spacing system based on a base unit with contextual application rules. |
| `typography-scale` | Create a modular typography scale with size, weight, and line-height relationships. |
| `visual-hierarchy` | Establish clear visual hierarchy through size, weight, color, spacing, and positioning. |
| `von-restorff-effect` | Apply the Von Restorff Effect to make the most important element distinctly different from its surroundings. |

## Origem

github.com/Owl-Listener/designer-skills — conteúdo preservado verbatim em `resources/`. Curadoria: novitckii.com/resources/claude-design-skills.
