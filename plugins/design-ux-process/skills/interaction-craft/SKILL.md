---
name: interaction-craft
description: "Design de interação — gateway pra 16 sub-skills do designer-skills (Owl Listener). Use SEMPRE que o trabalho envolver design de interação. Frases-gatilho: 'micro-interação', 'estados de loading', 'padrões de navegação', 'design de formulário', 'onboarding', 'tratamento de erro UX', 'feedback visual', 'gestos', 'Fitts', 'Hick', 'state machine'. Leia a sub-skill certa em resources/ antes de executar."
---

# Design de interação (interaction-craft)

Este gateway agrupa 16 sub-skills de **design de interação**. Cada sub-skill é um método completo e autocontido.

## Como usar

1. Identifique na tabela abaixo a(s) sub-skill(s) que casam com a tarefa.
2. Leia o arquivo `resources/<nome>/SKILL.md` (relativo a esta pasta) da(s) escolhida(s) — leia ANTES de produzir qualquer entregável.
3. Siga o método da sub-skill. Se duas se aplicam, leia ambas e combine.
4. Responda no idioma do usuário (PT-BR por padrão), mesmo que a sub-skill esteja em inglês.

## Sub-skills disponíveis

| Sub-skill | O que faz |
|---|---|
| `animation-principles` | Apply animation principles to UI motion for purposeful, polished interactions. |
| `doherty-threshold` | Apply the Doherty Threshold — keep system response times under 400ms to maintain user flow and perceived performance. |
| `error-handling-ux` | Design error prevention, detection, and recovery experiences. |
| `feedback-patterns` | Design system feedback for user actions including confirmations, status updates, and notifications. |
| `fitts-law` | Apply Fitts's Law to size and position interactive targets for fast, accurate interaction. |
| `form-design` | Design forms that minimize friction, prevent errors, and guide users to successful completion. |
| `gesture-patterns` | Design gesture-based interactions for touch and pointer devices. |
| `hicks-law` | Apply Hick's Law to reduce decision time by limiting the number of simultaneous choices presented to users. |
| `interfaces-that-feel` | Apply an emotional resonance lens to any UI. Use when a design is technically correct but flat — to identify what's missing and prescribe specific changes at the copy, motion, and interaction layer. |
| `loading-states` | Design loading, skeleton, and progressive content reveal patterns. |
| `micro-interaction-spec` | Specify micro-interactions with trigger, rules, feedback, and loop/mode definitions. |
| `millers-law` | Apply Miller's Law — chunk information into groups of ~4 to work within working memory limits. |
| `navigation-patterns` | Select and design navigation patterns that match product structure, user tasks, and platform conventions. |
| `onboarding-design` | Design first-run experiences that get users to value quickly without overwhelming them. |
| `search-ux` | Design search experiences that help users find what they need, recover from failure, and refine results. |
| `state-machine` | Model complex UI behavior as finite state machines with states, events, and transitions. |

## Origem

github.com/Owl-Listener/designer-skills — conteúdo preservado verbatim em `resources/`. Curadoria: novitckii.com/resources/claude-design-skills.
